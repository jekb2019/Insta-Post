import React from 'react';
import logo from '../../assets/images/header_logo.png';

const Auth = (props) => (
  <div>
    <img src={logo} alt="InstaPost logo" />
    <h1>Login / Signup Page</h1>
    <p>Will be implemented AWS Amplify UI later on</p>
  </div>
);

export default Auth;
